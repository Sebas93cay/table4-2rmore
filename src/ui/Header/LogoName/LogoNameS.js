import styled from 'styled-components';

export const LogoNameS = styled.div`
  color: ${(props) => props.theme.logo};
  font-weight: 900;
  font-size: 1.3rem;
  padding: 5px;
  text-align: center;
  margin-inline: 10px;
`;
